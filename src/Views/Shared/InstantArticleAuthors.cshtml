@using EPiServer.Core
@using EPiServer.Shell
@model IList<BVNetwork.InstantArticles.IInstantArticleAuthor>
@if (Model.Any())
{
    foreach (var author in Model)
    {
        Html.RenderPartial(@Paths.PublicRootPath + "BVNetwork.InstantArticles/Views/Shared/InstantArticleAuthor.cshtml", author);
    }
}